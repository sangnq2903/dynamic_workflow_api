name: dynamic_workflow_api
description: A sample command-line application with basic argument parsing.
version: 0.0.1
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ">=2.12.0 <3.0.0"

# Add regular dependencies here.
dependencies:
  args: ^2.4.2
  shelf: ^1.4.0
  shelf_router: ^1.1.3
dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
